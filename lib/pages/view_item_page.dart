
import 'package:flutter/material.dart';

class ViewItemPage extends StatelessWidget {
  const ViewItemPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('View Item Page'),
      ),
    );
  }
}